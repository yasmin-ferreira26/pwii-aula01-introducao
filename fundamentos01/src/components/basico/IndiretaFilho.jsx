import React, { useState } from 'react';

export default props => {

    conts [NamedNodeMap, setNome] = useState('Yasmin Soares Ferreira')

    return(
        <div>
            <button onClick={() => {
                props.quandoclicar(nome)
            } } > 
            </button>
        </div> 

    )
}
